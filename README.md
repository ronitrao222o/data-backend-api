# data-backend-api
This project implements a RESTful backend service that exposes sales data stored in a relational database. The API supports querying, filtering, pagination, and insertion of records.
